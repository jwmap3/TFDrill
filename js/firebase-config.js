/* ============================================================
   TFDrills — Firebase project configuration
   ------------------------------------------------------------
   Fill these in from your Firebase project:
   Firebase Console -> Project Settings (gear icon) -> scroll to
   "Your apps" -> the web app (</>) -> SDK setup and configuration.

   This file only holds public client identifiers (not secrets) —
   it's safe to commit, Firebase security is enforced by
   firestore.rules, not by hiding this config.
   ============================================================ */

const FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
